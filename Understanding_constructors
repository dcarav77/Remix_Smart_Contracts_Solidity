pragma solidity ^0.8.0; // SPDX-License-Identifier: MIT

contract Constructor {
    address public owner;
    uint public x;

    constructor (uint _x) {
        owner = msg.sender; // msg. sender is the account that deployed the contract 
        x = _x;  // set State Variable x to the input
    }
 }
